require 'spec_helper'

describe 'Upload Image' do
  it 'uploads image', js: true do
    # TODO 
    
    # visit new_post_path
    # page.should have_text('New post')

    # fill_in 'Title', with: 'Feature spec'
    # attach_file 'image[file]', Rails.root.join('..', 'support', 'images', 'picture.jpg')

    # page.should find(:xpath, "//img")
    # click_button 'Create Post'
    # page.should have_text('Post was successfully created.')
    # page.should have_text('Feature spec')
    # Post.count.should == 1
    # Post.first.picture.should_not be_nil
  end
end
